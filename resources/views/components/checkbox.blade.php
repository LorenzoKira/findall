<div class="flex items-center gap-2 checkbox-group">
    <input type="checkbox" name="{{ $name }}" id="{{ $id }}" >
    <label for="{{ $id }}" class="">J'accepte les <a href="#" class="border-b border-dashed term-link">termes et conditions</a></label>
</div>