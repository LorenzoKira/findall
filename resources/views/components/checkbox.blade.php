<div class="flex items-center gap-2 checkbox-group">
    <input type="checkbox" name="{{ $name }}" id="{{ $id }}" >
    <label for="{{ $id }}" class="">J'accepte <a href="#" class="border-b border-dashed term-link">les termes et conditions</a></label>
</div>